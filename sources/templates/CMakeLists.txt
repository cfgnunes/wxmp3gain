# ==============================================================================
# Templates.                                                                    
# ==============================================================================
# Set a variable containing the path to the template directory.
set(PROJECT_TEMPLATE_SOURCE_DIR ${CMAKE_CURRENT_SOURCE_DIR} CACHE INTERNAL "The path to the template source directory.")
set(PROJECT_TEMPLATE_OUTPUT_DIR ${CMAKE_CURRENT_BINARY_DIR} CACHE INTERNAL "The path to the template output directory.")

# Get the current year for the copyright information.
string(TIMESTAMP CURRENT_YEAR "%Y")
# Header template file with values generated by CMake to make it usable in the code.
configure_file(Constants.h.in ${CMAKE_GENERATED_SOURCES_FOLDER}/Constants.h)
